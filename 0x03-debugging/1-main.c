#include <stdio.h>
/**
 * main - infinite loop
 *
 * Return: 0
 */

int main(void)
{
int 1;

printf("infinite loop incoming :\n");

i = 0;

/*
 * while (i < 10)
 * {
 * infinite loop - no increase in variables
 * putchar(i);
 }
 */
printf("infinite loop avoided! :\n");
return (0);
}
