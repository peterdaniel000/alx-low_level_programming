#include "main.h"

/**
 * print_alphabet - entry point
 *
 * Return: 0 after run function
 */

void print_alphabet(void)
{
	char dan = 'a';

	for (dan < 'a'; dan < 'z'; dan++)
	{
		_putchar(dan);
	}
	return (0);
}
